Setup

This project uses the MERN stack. npm install the dependencies in both the client and server directories and then run npm start in each directory.

The db is running on mongodb atlas and you can login with either of the following two users within the app:

Email: test@test.com or batman@test.com
Password: qwerty123 
